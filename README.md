# BetterLife - 나의 라이프 로그

명상, 운동, 식사 등 일상의 모든 순간을 기록하고 시각화하는 라이프 로깅 웹 서비스입니다.

## 🌟 주요 기능

- **명상 기록**: 아이폰 단축어나 액션 버튼으로 명상 시작/종료를 빠르게 기록
- **확장 가능한 구조**: 운동, 식사, 심박수, 수면 등 다양한 활동 타입 지원
- **실시간 대시보드**: 기록된 데이터를 카드 형식으로 시각화
- **외부 연동**: iPhone Shortcuts, Garmin Connect 등과 연동 가능

## 🚀 시작하기

### 설치

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

### 프로덕션 빌드

```bash
npm run build
npm start
```

## 📱 아이폰 단축어 연동 방법

### 1. 단축어 앱에서 새 단축어 만들기

1. iPhone에서 **단축어(Shortcuts)** 앱을 엽니다
2. **+** 버튼을 눌러 새 단축어를 만듭니다
3. **작업 추가**를 선택합니다

### 2. URL 내용 가져오기 설정

1. 검색창에 "URL 내용 가져오기"를 입력하고 선택합니다
2. 다음과 같이 설정합니다:

**URL 설정:**
```
http://your-server-ip:3000/api/log
```

**방법(Method):**
```
POST
```

**헤더(Headers):**
```
Content-Type: application/json
```

**요청 본문(Request Body):**
```json
{
  "user_id": "your_user_id",
  "action": "meditation_start",
  "timestamp": "현재 날짜"
}
```

### 3. 현재 날짜 포맷팅

1. "현재 날짜 가져오기" 작업을 추가합니다
2. "날짜 포맷" 작업을 추가하고 다음과 같이 설정:
   - 포맷: **사용자 설정**
   - 포맷 문자열: `yyyy-MM-dd'T'HH:mm:ssZZZZZ`

### 4. 액션 버튼에 할당 (iPhone 15 Pro 이상)

1. **설정** > **동작 버튼**으로 이동
2. **단축어**를 선택
3. 생성한 단축어를 선택

이제 액션 버튼을 누르면 바로 명상 시작이 기록됩니다!

## 🔌 API 사용법

### 이벤트 기록하기

**Endpoint:** `POST /api/log`

**Request Body:**
```json
{
  "user_id": "user123",
  "action": "meditation_start",
  "timestamp": "2025-12-31T12:00:00+09:00",
  "metadata": {
    "note": "오전 명상"
  }
}
```

**지원하는 action 값:**
- `meditation_start` - 명상 시작
- `meditation_end` - 명상 종료
- `meal` - 식사 기록
- `heart_rate` - 심박수 기록
- `exercise` - 운동 기록
- `sleep` - 수면 기록
- `mood` - 기분 기록

**Response:**
```json
{
  "success": true,
  "data": {
    "id": "1735632000000-abc123",
    "user_id": "user123",
    "activity_type": "MEDITATION_START",
    "timestamp": "2025-12-31T12:00:00+09:00",
    "metadata": {
      "note": "오전 명상"
    },
    "created_at": "2025-12-31T12:00:01+09:00"
  }
}
```

### 이벤트 조회하기

**Endpoint:** `GET /api/events`

**Query Parameters:**
- `user_id` - 사용자 ID로 필터링
- `activity_type` - 활동 타입으로 필터링 (예: MEDITATION_START)
- `start_date` - 시작 날짜 (ISO-8601)
- `end_date` - 종료 날짜 (ISO-8601)

**Example:**
```bash
curl "http://localhost:3000/api/events?user_id=user123&activity_type=MEDITATION_START"
```

## 🔧 기술 스택

- **Frontend**: Next.js 15 (App Router), React 19, TypeScript
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **Date Formatting**: date-fns
- **Database**: JSON 파일 기반 (추후 Supabase 연동 가능)

## 📁 프로젝트 구조

```
BetterLife/
├── app/
│   ├── api/
│   │   ├── log/route.ts          # 이벤트 기록 API
│   │   └── events/route.ts       # 이벤트 조회 API
│   ├── globals.css               # 전역 스타일
│   ├── layout.tsx                # 루트 레이아웃
│   └── page.tsx                  # 메인 대시보드
├── components/
│   ├── MeditationCard.tsx        # 명상 기록 카드
│   └── StatsCard.tsx             # 통계 카드
├── lib/
│   └── db.ts                     # 데이터베이스 유틸리티
├── types/
│   └── index.ts                  # TypeScript 타입 정의
└── data/
    └── events.json               # 이벤트 데이터 저장소
```

## 🎯 향후 확장 계획

### 데이터 소스 추가
- [ ] Garmin Connect 연동 (심박수, 운동 데이터)
- [ ] Apple Health 연동
- [ ] 식사 사진 업로드 및 AI 분석
- [ ] 수면 패턴 추적

### 기능 개선
- [ ] 사용자 인증 시스템
- [ ] 데이터 시각화 차트 (주간/월간 통계)
- [ ] 목표 설정 및 달성률 추적
- [ ] 데이터 내보내기 (CSV, JSON)

### 데이터베이스 마이그레이션
현재는 JSON 파일을 사용하지만, Supabase로 쉽게 마이그레이션할 수 있습니다:

1. Supabase 프로젝트 생성
2. `events` 테이블 생성:
```sql
CREATE TABLE events (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  activity_type TEXT NOT NULL,
  timestamp TIMESTAMPTZ NOT NULL,
  metadata JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```
3. `lib/db.ts`를 Supabase 클라이언트로 교체

## 📄 라이선스

MIT License

## 🤝 기여

이슈와 PR은 언제나 환영합니다!
